import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fdfdfd', // background
    justifyContent: 'center',
    alignItems: 'center',
  },
  title: {
    fontSize: 40,
    fontWeight: 'bold',
    color: '#ff4c60', // primary
  },
  subtitle: {
    fontSize: 18,
    color: '#ff9f43', // secondary text
    marginTop: 8,
  },
});
